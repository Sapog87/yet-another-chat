global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'service-scrape'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: [ 'service:8080' ]
    basic_auth:
      username: 'prometheus'
      password: 'prometheus'